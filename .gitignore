.venv/
__pycache__/
*.pyc

# data and outputs stay out of git (large binaries)
data/
outputs/
*.ply
*.zip
*.pt
*.pth
*.onnx

# exception: the published fine-tuned weights are shared via the repo
!models/bins_latest.pt
